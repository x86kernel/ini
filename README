ini
===

Parses simple ini files into a linked list

Options
===

```
-c, --comment : parse comment and show all of them
c.f) comment symbol : ';', '#'

-a, --printall : print all of empty settings 
e.x)in file, this_is_hidden_setting = 
print as ---> this_is_hidden_setting = [IS_EMPTY]
```
